<Page
    x:Class="PomodoroAssistant.Views.ReportsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PomodoroAssistant.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" RequestedTheme="Dark" x:Name="pageReports" SizeChanged="pageReports_SizeChanged">

    <Grid>
        <Hub x:Name="hubTasks" Margin="0 -10 0 0" HorizontalContentAlignment="Stretch">
            <HubSection Header="aaa" HorizontalContentAlignment="Stretch">
                <DataTemplate>
                    <Grid MinWidth="270" Width="{Binding SectionWidth}">

                    </Grid>
                </DataTemplate>
            </HubSection>

            <HubSection Header="bbb" HorizontalContentAlignment="Stretch">
                <DataTemplate>
                    <Grid MinWidth="270" Width="{Binding SectionWidth}">

                    </Grid>
                </DataTemplate>
            </HubSection>

            <HubSection Header="ccc" HorizontalContentAlignment="Stretch">
                <DataTemplate>
                    <Grid MinWidth="270" Width="{Binding SectionWidth}">

                    </Grid>
                </DataTemplate>
            </HubSection>
        </Hub>
    </Grid>
</Page>
